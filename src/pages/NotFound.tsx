import {Link,useLocation} from 'react-router-dom';import {Mark} from '../components/Layout';
export default function NotFound(){const {pathname}=useLocation();const hint=pathname.startsWith('/opportunities/')?pathname.split('/').pop()||'':'';return <section className="not-found"><div className="radar"><Mark/></div><div><p className="kicker">404 · PAGE NOT FOUND</p><h1>That opportunity isn’t here.</h1><p>The link may be outdated, the listing may have moved, or the page may never have existed.</p><div className="actions">{hint&&<Link className="button" to={`/opportunities?q=${encodeURIComponent(hint)}`}>Search opportunities</Link>}<Link className="button secondary" to="/opportunities">Browse all opportunities</Link><Link to="/">Return home</Link><Link to="/submit?type=broken">Report a broken link</Link></div></div></section>}
