export default function SignIn(){return <section className="form-page"><p className="kicker">OPTIONAL ACCOUNT</p><h1>Sign in</h1><div className="alert"><strong>Local demo mode</strong> — cloud authentication and email are not configured. Browsing and on-device saves still work.</div><form onSubmit={e=>e.preventDefault()}><label>Email<input type="email" disabled placeholder="student@example.org"/></label><button className="button" disabled>Send secure sign-in link</button><p className="muted">Set the Supabase environment variables to enable real magic-link authentication. The app never pretends a message was sent.</p></form></section>}
