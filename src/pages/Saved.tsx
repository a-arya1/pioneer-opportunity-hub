import data from '../data/opportunities.json';import type {Opportunity} from '../types';import OpportunityCard from '../components/OpportunityCard';import {useSaved} from '../hooks/useSaved';
export default function Saved(){const saves=useSaved();const rows=(data as Opportunity[]).filter(x=>saves.saved.includes(x.id));const download=()=>{const blob=new Blob([JSON.stringify(rows,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='pioneer-opportunities.json';a.click();URL.revokeObjectURL(a.href)};return <section className="section narrow"><div className="page-heading"><p className="kicker">PRIVATE ON THIS DEVICE</p><h1>Saved opportunities</h1><p>In demo mode, saves stay in this browser. Sign in is required to sync across devices.</p></div>{rows.length?<><div className="toolbar"><button className="button secondary" onClick={download}>Export saved items</button><button className="text-button" onClick={saves.clear}>Clear saved items</button></div>{rows.map(o=><OpportunityCard key={o.id} o={o} saved onSave={()=>saves.toggle(o.id)}/>)}</>:<div className="empty"><h2>Nothing saved yet</h2><p>Save a listing to compare it or return later.</p><a className="button" href="/opportunities">Browse opportunities</a></div>}</section>}
