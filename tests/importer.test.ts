import {describe,it,expect} from 'vitest';import data from '../src/data/opportunities.json';
describe('workbook import',()=>{it('imports at least 150 valid source-backed listings while withholding ambiguous rows',()=>{expect(data.length).toBeGreaterThanOrEqual(150);expect(data.every(x=>x.sourceUrl&&x.lastVerified&&x.confidence&&x.status)).toBe(true);expect(data.filter(x=>x.status==='verify').every(x=>!x.published)).toBe(true)});it('has stable unique ids and slugs',()=>{expect(new Set(data.map(x=>x.id)).size).toBe(data.length);expect(new Set(data.map(x=>x.slug)).size).toBe(data.length)})});
